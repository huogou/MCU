/* 角色详情 character · V1.1 Step6 · 深色宇宙科技风，全 Token 化 */
.mcu-page { min-height: 100vh; background: var(--bg); padding: 0 32rpx; box-sizing: border-box; }

/* —— Hero 区 —— */
.hero { padding: 48rpx 0 40rpx; display: flex; flex-direction: column; align-items: center; text-align: center; }
.hero-avatar { width: 128rpx; height: 128rpx; border-radius: 50%; border: 4rpx solid; font-size: 60rpx; font-weight: 700; display: flex; align-items: center; justify-content: center; }
.hero-cn { font-size: 40rpx; font-weight: 700; color: var(--text-main); margin-top: 24rpx; line-height: 1.3; }
.hero-en { font-size: 24rpx; color: var(--text-sub); margin-top: 8rpx; }
.hero-meta { display: flex; align-items: center; gap: 16rpx; margin-top: 20rpx; }
.hero-camp { font-size: 22rpx; padding: 6rpx 20rpx; border-radius: 999rpx; }
.hero-count { font-size: 22rpx; color: var(--text-weak); }

/* —— 通用卡片 —— */
.card { background: var(--surface-2); border: 1rpx solid var(--surface-3); border-radius: 20rpx; padding: 28rpx; margin-bottom: 24rpx; }
.card-title { font-size: 26rpx; font-weight: 600; color: var(--gold); margin-bottom: 20rpx; letter-spacing: 1rpx; }
.char-note { font-size: 26rpx; color: var(--text-sub); line-height: 1.7; }

/* —— 首次出现 / 关联作品 行 —— */
.first-row, .film-row { display: flex; align-items: center; }
.row-hover { opacity: 0.7; }
.film-letter { width: 72rpx; height: 72rpx; border-radius: 14rpx; color: var(--white); font-size: 32rpx; font-weight: 700; display: flex; align-items: center; justify-content: center; margin-right: 20rpx; flex-shrink: 0; }
.film-info { flex: 1; min-width: 0; }
.film-cn { font-size: 28rpx; color: var(--text-main); font-weight: 600; }
.film-en { font-size: 20rpx; color: var(--text-sub); margin-top: 4rpx; }
.phase-tag { font-size: 20rpx; color: var(--text-weak); border: 1rpx solid var(--surface-3); border-radius: 10rpx; padding: 6rpx 14rpx; flex-shrink: 0; }
.film-status { font-size: 20rpx; padding: 6rpx 16rpx; border-radius: 20rpx; flex-shrink: 0; }
.status-unwatched { color: var(--gold); background: var(--gold-a15); }
.status-watching { color: var(--success); background: var(--success-a15); }
.status-watched { color: var(--text-weak); background: var(--surface-3); }

/* —— 关联作品列表 —— */
.film-list { display: flex; flex-direction: column; gap: 20rpx; }

/* —— 关系探索：关联角色网格 —— */
.related-grid { display: flex; flex-wrap: wrap; gap: 20rpx; }
.related-item { width: calc((100% - 40rpx) / 3); background: var(--surface-1); border: 1rpx solid var(--surface-3); border-radius: 18rpx; padding: 24rpx 12rpx; display: flex; flex-direction: column; align-items: center; text-align: center; box-sizing: border-box; }
.related-hover { border-color: var(--gold); }
.related-avatar { width: 80rpx; height: 80rpx; border-radius: 50%; border: 2rpx solid; font-size: 34rpx; font-weight: 700; display: flex; align-items: center; justify-content: center; }
.related-cn { font-size: 24rpx; font-weight: 600; color: var(--text-main); margin-top: 14rpx; line-height: 1.3; }
.related-shared { font-size: 19rpx; color: var(--text-weak); margin-top: 6rpx; }

/* —— 数据不存在兜底 —— */
.notfound-page { display: flex; flex-direction: column; align-items: center; padding-top: 200rpx; }
.nf-icon { width: 140rpx; height: 140rpx; border-radius: 50%; background: var(--surface-2); border: 1rpx solid var(--surface-3); color: var(--text-weak); font-size: 64rpx; display: flex; align-items: center; justify-content: center; }
.nf-title { font-size: 34rpx; font-weight: 600; color: var(--text-main); margin-top: 32rpx; }
.nf-desc { font-size: 24rpx; color: var(--text-sub); margin-top: 16rpx; text-align: center; }
.nf-btn { margin-top: 48rpx; background: var(--gold); color: var(--gold-btn-text); font-weight: 600; border-radius: 999rpx; padding: 20rpx 80rpx; font-size: 28rpx; }

.bottom-pad { height: 60rpx; }
