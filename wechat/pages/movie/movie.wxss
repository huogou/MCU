/* 电影详情 movie 样式 · V1.2 统一视觉
 * 颜色全部引用 app.wxss 全局 Token（零 raw hex 泄漏）
 * 图标：纯 CSS 绘制（ico / chevron），不引入 emoji 与第三方图标库
 */

.movie-page {
  padding: 0;
}

/* 海报阶段色系统已统一至 app.wxss 全局（Step4）*/

/* ══════ 导航栏 ══════ */
.movie-nav-bar {
  display: flex;
  align-items: center;
  gap: var(--space-sm);
  padding: var(--space-sm) var(--page-x) var(--space-xs);   /* 12→8 */
}
.back-btn {
  width: 56rpx;
  height: 56rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}
.nav-label {
  font-size: var(--fs-body);
  color: var(--text-sub);
}

/* ══════ ① Hero 区 ══════ */
.movie-hero {
  position: relative;
  padding: var(--space-lg) var(--page-x) var(--space-xl);
  display: flex;
  gap: var(--space-lg);
}
.hero-bg {
  position: absolute;
  inset: 0;
  pointer-events: none;
  background-size: cover;
  background-position: center;
}
.poster {
  width: 220rpx;
  height: 320rpx;
  border-radius: var(--radius-md);
  overflow: hidden;
  border: 4rpx solid var(--gold-a20);
  flex-shrink: 0;
  position: relative;
  z-index: 1;
}
.poster-img { width: 100%; height: 100%; }
.poster-fill {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
}
.poster-letter {
  font-size: var(--fs-display);
  font-weight: 700;
  color: var(--white-a50);
  text-align: center;
  line-height: 1.1;
}
.hero-info {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  position: relative;
  z-index: 1;
  min-width: 0;
}
.hero-phase {
  display: flex;
  align-items: center;
  gap: 12rpx;
  margin-bottom: var(--space-xs);   /* 12→8 */
}
.hero-phase-dot {
  width: 8rpx;
  height: 32rpx;
  border-radius: 4rpx;
}
.hero-phase-text {
  font-size: var(--fs-mini);
  color: var(--text-weak);
}
.hero-title {
  font-size: var(--fs-title);
  font-weight: 700;
  color: var(--text-main);
  line-height: 1.3;
  margin-bottom: var(--space-xs);
}
.hero-en {
  font-size: var(--fs-caption);
  color: var(--text-weak);
  margin-bottom: var(--space-sm);
}
.hero-chips {
  display: flex;
  gap: 10rpx;
  flex-wrap: wrap;
}
.chip {
  display: inline-block;
  padding: 6rpx 16rpx;
  border-radius: var(--radius-full);
  border: 2rpx solid var(--surface-3);
  font-size: var(--fs-mini);
  color: var(--text-weak);
}
.chip.gold { color: var(--gold); border-color: var(--gold-a20); background: var(--gold-a06); }
.chip.red { color: var(--p5); border-color: var(--p5-a20); background: var(--p5-a06); }
.chip.blue { color: var(--p1); border-color: var(--p1-a20); background: var(--p1-a06); }

/* 状态标签 */
.watch-status {
  display: inline-flex;
  align-items: center;
  gap: var(--space-xs);
  padding: var(--space-xs) var(--space-sm);
  border-radius: var(--radius-full);
  font-size: var(--fs-mini);
  font-weight: 600;
  margin-top: var(--space-sm);
  align-self: flex-start;
}
.watch-status.unwatched {
  background: var(--gold-a10);
  color: var(--gold);
  border: 2rpx solid var(--gold-a20);
}
.watch-status.watching {
  background: var(--success-a10);
  color: var(--success);
  border: 2rpx solid var(--success-a20);
}
.watch-status.watched {
  background: var(--surface-3);
  color: var(--text-weak);
  border: 2rpx solid var(--surface-3);
}

/* ══════ ② 主 CTA 区 ══════ */
.movie-cta-section {
  padding: 0 var(--page-x);
  margin-bottom: var(--space-md);
}
.movie-cta {
  width: 100%;
  height: 88rpx;
  font-size: var(--fs-body);
  font-weight: 600;
  border: none;
  border-radius: var(--radius-md);
  display: flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-sm);
  box-sizing: border-box;
}
.movie-cta.primary { background: var(--gold); color: var(--gold-btn-text); box-shadow: var(--glow-gold); }
.movie-cta.done {
  background: var(--surface-3);
  color: var(--text-weak);
  border: 2rpx solid var(--surface-3);
}

/* ══════ ③ 观看资源模块 ══════ */
.resource-module {
  margin: 0 var(--page-x) var(--space-lg);
  background: var(--surface-2);
  border: 2rpx solid var(--surface-3);
  border-radius: var(--radius-md);
  overflow: hidden;
}
.resource-header {
  padding: var(--space-md);
  display: flex;
  align-items: center;
  gap: var(--space-sm);
}
.resource-info { flex: 1; min-width: 0; }
.resource-title {
  font-size: var(--fs-body);
  font-weight: 600;
  color: var(--text-main);
  margin-bottom: var(--space-xs);   /* 6→8 */
}
.resource-sub {
  font-size: var(--fs-mini);
  color: var(--text-weak);
}
.resource-arrow {
  width: 16rpx;
  height: 16rpx;
  border-right: 3rpx solid var(--text-weak);
  border-bottom: 3rpx solid var(--text-weak);
  transform: rotate(45deg);
  transition: transform 0.2s;
  flex-shrink: 0;
}
.resource-module.expanded .resource-arrow {
  transform: rotate(225deg);
}
.resource-body {
  display: none;
  padding: 0 var(--space-md) var(--space-md);
  border-top: 2rpx solid var(--surface-3);
  margin: 0 var(--space-md);
  padding-top: var(--space-md);
}
.resource-module.expanded .resource-body {
  display: block;
}
.resource-note {
  font-size: var(--fs-mini);
  color: var(--text-sub);
  line-height: 1.6;
}

/* ══════ ④ 为什么现在看 ══════ */
.why-section {
  margin: 0 var(--page-x) var(--space-lg);
  background: var(--surface-2);
  border: 2rpx solid var(--surface-3);
  border-radius: var(--radius-lg);
  padding: var(--space-md);
}
.why-label {
  font-size: var(--fs-caption);
  font-weight: 600;
  color: var(--accent-blue);
  margin-bottom: var(--space-xs);   /* 12→8 */
}
.why-ctx {
  font-size: var(--fs-mini);
  color: var(--gold);
  margin-bottom: var(--space-xs);   /* 10→8 */
  line-height: 1.5;
}
.why-text {
  font-size: var(--fs-body);
  color: var(--text-sub);
  line-height: 1.7;
}

/* ══════ ④-2 主要角色（M-08/M-09 新增） ══════ */
.cast-section {
  margin: 0 var(--page-x) var(--space-lg);
}
.cast-row {
  display: flex;
  gap: var(--space-md);
}
.cast-card {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  background: var(--surface-2);
  border: 2rpx solid var(--surface-3);
  border-radius: var(--radius-md);
  padding: var(--space-sm) 12rpx;
  box-sizing: border-box;
}
.cast-avatar {
  width: 80rpx;
  height: 80rpx;
  border-radius: 50%;
  overflow: hidden;
  border: 3rpx solid transparent;        /* 由 fring-* 覆盖阵营色 */
  display: flex;
  align-items: center;
  justify-content: center;
}
.cast-initial {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: var(--fs-body);             /* 28rpx 首字 */
  font-weight: 700;
  color: var(--white-a70);
}
.cast-name {
  font-size: var(--fs-mini);             /* 22rpx */
  font-weight: 600;
  color: var(--text-main);
  margin-top: 12rpx;
  text-align: center;
}
.cast-faction {
  font-size: var(--fs-mini);             /* 22rpx */
  font-weight: 400;
  margin-top: var(--space-xs);           /* 4→8（P1 间距统一） */
  text-align: center;
}

/* 阵营色系统已统一至 app.wxss 全局（Step4）*/

/* ══════ ⑤ 前后关联 ══════ */
.sequence-section {
  padding: 0 var(--page-x);
  margin-bottom: var(--space-lg);
}
.sequence-label {
  font-size: var(--fs-caption);
  font-weight: 600;
  color: var(--text-weak);
  margin-bottom: var(--space-sm);
}
.sequence-row {
  display: flex;
  gap: var(--space-sm);
  align-items: stretch;
}
.seq-card {
  flex: 1;
  background: var(--surface-2);
  border: 2rpx solid var(--surface-3);
  border-radius: var(--radius-md);
  padding: var(--space-sm);
  text-align: center;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: var(--space-xs);   /* 12→8 */
}
.seq-card.current {
  background: var(--gold-a10);
  border-color: var(--gold-a20);
}
.seq-card.seq-placeholder {
  justify-content: center;
}
.seq-poster {
  width: 80rpx;
  height: 120rpx;
  border-radius: var(--radius-sm);
  overflow: hidden;
  display: flex;
  align-items: center;
  justify-content: center;
}
.seq-poster text {
  font-size: var(--fs-body);
  font-weight: 600;   /* 700→600（占位首字 P1-7） */
  color: var(--white-a50);
}
/* 图片填满容器（前后关联 / 下一部 / 角色头像统一）——全局 app.wxss .fill-img */
.seq-name {
  font-size: var(--fs-mini);
  color: var(--text-sub);
  line-height: 1.3;
}
.seq-card.current .seq-name {
  color: var(--gold);
  font-weight: 600;
}
.seq-role {
  font-size: var(--fs-mini);
  color: var(--text-weak);
}
.seq-arrow {
  display: flex;
  align-items: center;
  color: var(--text-weak);
  flex-shrink: 0;
}

/* ══════ ⑥ 看完之后 ══════ */
.after-section {
  margin: 0 var(--page-x) var(--space-xl);
}
.mark-watched-btn {
  width: 100%;
  padding: var(--space-md) 0;
  font-size: var(--fs-body);
  font-weight: 600;
  background: var(--surface-2);
  color: var(--success);
  border: 2rpx solid var(--success-a20);
  border-radius: var(--radius-md);
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12rpx;
  box-sizing: border-box;
  margin-bottom: var(--space-md);
}
.mark-watched-btn.marked {
  background: var(--success-a08);
  border-color: var(--success-a30);
  color: var(--success);
}
.next-rec {
  background: var(--surface-2);
  border: 2rpx solid var(--surface-3);
  border-radius: var(--radius-md);
  padding: var(--space-md);
  display: flex;
  gap: var(--space-md);
}
.next-rec-poster {
  width: 100rpx;
  height: 144rpx;
  border-radius: var(--radius-sm);   /* Bug 修正：--space-xs(8)→--radius-sm(12)，P1-4 */
  overflow: hidden;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
}
.next-rec-poster text {
  font-size: var(--fs-title);
  font-weight: 600;   /* 700→600（占位首字 P1-7） */
  color: var(--white-a50);
}
.next-rec-info {
  flex: 1;
  min-width: 0;
}
.next-rec-label {
  font-size: var(--fs-mini);
  color: var(--gold);
  font-weight: 600;
  margin-bottom: var(--space-xs);
}
.next-rec-title {
  font-size: var(--fs-body);
  font-weight: 600;
  color: var(--text-main);
  margin-bottom: var(--space-xs);   /* 6→8 */
}
.next-rec-desc {
  font-size: var(--fs-caption);
  color: var(--text-sub);
  line-height: 1.4;
}
.next-rec-arrow {
  display: flex;
  align-items: center;
  color: var(--text-weak);
  flex-shrink: 0;
}

/* ══════ 未找到 ══════ */
.movie-empty {
  padding: 160rpx var(--page-x);
  text-align: center;
}
.movie-empty-title {
  font-size: var(--fs-title);
  color: var(--text-main);
  margin-bottom: 12rpx;
}
.movie-empty-sub {
  font-size: var(--fs-caption);
  color: var(--text-weak);
}

/* ══════ CSS 图标 ══════ */
.ico {
  display: inline-flex;
  align-items: center;
  justify-content: center;
}
.ico-circle {
  width: 12rpx;
  height: 12rpx;
  border: 2rpx solid currentColor;
  border-radius: 50%;
  box-sizing: border-box;
}
.ico-play {
  width: 0;
  height: 0;
  border-style: solid;
  border-width: 14rpx 0 14rpx 24rpx;
  border-color: transparent transparent transparent currentColor;
}
.ico-check {
  width: 14rpx;
  height: 16rpx;
  position: relative;
}
.ico-check::after {
  content: '';
  position: absolute;
  left: 1rpx;
  top: 1rpx;
  width: 6rpx;
  height: 11rpx;
  border-right: 3rpx solid currentColor;
  border-bottom: 3rpx solid currentColor;
  transform: rotate(45deg);
}
.chevron-left {
  width: 16rpx;
  height: 16rpx;
  border-left: 3rpx solid var(--text-sub);
  border-bottom: 3rpx solid var(--text-sub);
  transform: rotate(45deg);
}
.chevron-right {
  width: 14rpx;
  height: 14rpx;
  border-right: 3rpx solid currentColor;
  border-bottom: 3rpx solid currentColor;
  transform: rotate(-45deg);
}

/* ══════ 成就解锁弹窗 ══════ */
.ach-mask {
  position: fixed;
  inset: 0;
  background: var(--bg-a70);
  z-index: 99;
  display: flex;
  align-items: flex-end;
}
.ach-sheet {
  width: 100%;
  background: var(--surface-1);
  border-radius: 32rpx 32rpx 0 0;
  padding: var(--space-xl) 48rpx 60rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  position: relative;
}
.ach-close {
  position: absolute;
  top: 28rpx;
  right: var(--space-lg);
  width: 56rpx;
  height: 56rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}
.ach-close-x {
  width: 24rpx;
  height: 24rpx;
  position: relative;
}
.ach-close-x::before,
.ach-close-x::after {
  content: '';
  position: absolute;
  left: 0;
  top: 50%;
  width: 100%;
  height: 3rpx;
  background: var(--text-weak);
  border-radius: 2rpx;
}
.ach-close-x::before { transform: rotate(45deg); }
.ach-close-x::after { transform: rotate(-45deg); }
.ach-sheet-badge {
  width: 136rpx;
  height: 136rpx;
  border-radius: 50%;
  background: var(--gold-a14);
  border: 3rpx solid var(--gold-a55);
  color: var(--gold);
  font-size: 60rpx;
  font-weight: 700;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: var(--space-md);
}
.ach-sheet-title {
  font-size: var(--fs-caption);
  color: var(--text-weak);
  letter-spacing: 0.06em;
  margin-bottom: 12rpx;
}
.ach-sheet-name {
  font-size: var(--fs-display-sm);
  font-weight: 700;
  color: var(--text-main);
  margin-bottom: 12rpx;
}
.ach-sheet-desc {
  font-size: var(--fs-body);
  color: var(--text-sub);
  margin-bottom: 44rpx;
  text-align: center;
  line-height: 1.5;
}
.ach-sheet-actions {
  width: 100%;
  display: flex;
  gap: var(--space-sm);
}
.ach-btn {
  flex: 1;
  height: 88rpx;
  line-height: 88rpx;
  text-align: center;
  font-size: var(--fs-body);
  font-weight: 600;
  border-radius: 44rpx;
  background: var(--surface-2);
  color: var(--text-main);
  border: 2rpx solid var(--surface-3);
}
.ach-btn.gold {
  background: var(--gold);
  color: var(--gold-btn-text);
  border: none;
}
