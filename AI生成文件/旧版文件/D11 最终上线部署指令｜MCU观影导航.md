# D11 最终上线部署

## 一、当前状态

D11 最终验收已经完成。

### 开发验收
通过。

- 数据唯一性通过
- 功能测试 52/52 通过
- 异常测试通过
- H5 承接通过
- 观看资源入口通过
- 性能检查通过

### 设计验收
B｜小问题，不阻塞上线。

当前发现的 Token 硬编码问题属于非阻塞视觉一致性问题，记录进入后续 D12，不影响本次上线。

---

# 二、本次任务

现在进入：

> **正式部署上线**

本次不再新增功能。

不修改产品结构。

不修改 MCU 数据。

不修改 H5。

不修改页面视觉。

不处理 D12 Token 优化。

---

# 三、上线前最后检查

部署前只做一次最终生产环境检查：

1. 构建是否正常；
2. 生产环境配置是否正确；
3. CloudBase 环境是否正确；
4. 数据读取是否正常；
5. H5 → 小程序承接是否正常；
6. 电影详情是否正常；
7. 观看资源复制链接是否正常；
8. 已看状态是否正常；
9. 路线进度是否正常；
10. 下一部推荐是否正常；
11. “我要吐槽”入口是否正常；
12. TabBar 是否正常；
13. 页面返回是否正常。

如果以上全部正常：

> **直接部署正式环境。**

---

# 四、部署原则

继续使用当前正式项目。

禁止：

- 新建第二套小程序项目；
- 新建第二套 MCU 数据；
- 修改 H5；
- 重构数据架构；
- 新增功能；
- 顺手修改页面；
- 顺手处理 D12 视觉问题。

---

# 五、部署完成后提交

部署完成后只提交：

### 1. 部署结果

明确：

> 已正式部署 / 未部署

### 2. 正式访问方式

提供当前正式环境访问入口。

### 3. 最终自测结果

明确：

- 页面加载
- 数据读取
- TabBar
- 电影详情
- 观看资源
- 已看状态
- 路线进度
- 下一部推荐
- H5 承接
- 我要吐槽

是否全部正常。

### 4. 当前遗留问题

明确记录：

> D12：视觉 Token 统一问题。

除此之外，不再自行增加新的问题范围。

---

# 六、停止条件

正式部署完成并确认生产环境正常后：

> **停止 D11。**

不要自行进入 D12。

等待策划最终确认后，再决定后续工作。

本次目标只有一个：

> **把当前已经完成并验收通过的 MCU 观影导航正式上线。**