# MCU观影导航 · 小程序源码恢复评估报告
（D12-A 第一步交付物）
生成时间：2026-08-20 17:15
执行：开发/设计 AI（WorkBuddy，已合并双岗） ｜ 汇报：策划 AI（deepseek）

> 指令要求：检查顺序 ①微信公众平台 ②微信开发者工具 ③云开发环境 ④其他位置；输出评估报告，含「已检查位置 / 找到内容 / 可恢复程度 / 是否建议恢复 / 无法恢复时重建成本评估」。

---

## 一、已检查位置（按指令顺序）

### ① 微信公众平台（需用户登录，AI 无法直连）→ 标记为「待用户核查」
AI 无公众平台凭证，无法代查。提供精确检查清单，恢复概率最高，建议优先：
- [ ] 版本管理 → 查看「开发版本 / 体验版本 / 线上版本」
- [ ] 代码包管理 → 是否有可下载的历史上传代码包
- [ ] 成员与权限 → 确认原开发者账号是否仍可登录
- [ ] 第三方授权 → 是否授权过代码托管 / CI 工具
说明：平台侧通常保留最近上传的代码包，是**唯一可能存有真源码的位置**。

### ② 微信开发者工具（本机）
- `C:\Users\Administrator\AppData\Local\微信开发者工具` → 仅工具本体 + WeappPlugin，无导入项目
- `D:\微信web开发者工具` → 仅工具本体安装，无用户项目
- 全盘扫描 `.wxml` / `.wxss` / `app.json` / `project.config.json`（用户项目）→ 0 命中
- AppID `wx78f00e7f0a5948b7` 缓存痕迹扫描 → 0 命中

### ③ 云开发环境（CloudBase 环境 mcu-d6gw0brqoa9521b58）
- 云函数：**0 个**
- 云存储：根目录清单 **0 个文件** → 无前端打包 / 部署痕迹
- 数据库：2 个集合 `feedback`（0 条）、`resources`（0 条）→ 均为运行时数据，非源码
- 结论：云端无小程序前端源码痕迹

### ④ 其他位置
- Git / Gitee / GitHub：本机无任何 `.git` 仓库；远程仓库需凭证，未连接
- 压缩包：全机搜索 `mcu/mini/小程序` 关键词 zip/rar/7z → 0 命中
- 云盘：百度网盘同步目录（`/d/百度网盘/BaiduNetdisk`、`/d/BaiduNetdiskDownload`）→ 无 MCU 内容
- OneDrive / Documents / Desktop → 无 MCU 小程序目录
- WPS 云备份 `三端小程序`、`D:\SEO\三端小程序` → 经核实为「**易必德数智招采**」产品（微信/支付宝/百度三端），**非本项目**
- 全盘 `.wxml` / `.wxss` 特征文件扫描 → 0 命中

---

## 二、找到内容
- 未找到任何小程序前端源码实体（`.wxml` / `.wxss` / 页面 `.js` / `app.json` 等）。
- 找到**重建依据文档齐全**：D7→D11 开发史、页面清单（8 页）、数据模型（4 个）、权威设计 Token、D10-A/B 设计原型、D11 验收报告（52/52）。
- 云端已就绪运行时数据层：空 `feedback` / `resources` 集合。

## 三、可恢复程度
- **直接恢复：不可行（0%）**。本机 / 云端 / 云盘 / 仓库均无源码实体。
- **间接恢复：可能（取决于①公众平台）**。若公众平台仍保留上传代码包，可直接拉回真源码——此为唯一未知变量。

## 四、是否建议恢复
- 立即执行 ① 微信公众平台核查（用户侧，约 10 分钟可完成）。
- 若公众平台有代码包 → 恢复真源码（最优路径）。
- 若公众平台也无 → **不建议继续耗时寻找，直接据文档重建**（见下，成本低且确定）。

## 五、若无法恢复——重建成本评估
- **性质：重建而非重设计**。产品 / 数据 / 设计已冻结，仅需按文档重写 WXML/WXSS/JS。
- **范围**：8 页面（home / routes / route-detail / browse / movie / explore / panorama / my-mcu）+ 4 TabBar + 4 模型（mcuData / userState / recommend / pano）。
- **数据**：单一源直接复用 H5 `mcu-navigator/data/*.js`（CONTENT 59 / RELATIONS 93 / ROUTES 11 / PANO 40+41），零数据迁移。
- **已有资产**：D10-A 设计原型 + 权威 Token + D11 验收清单（52/52 可对照），显著降低返工。
- **工作量估算**：核心 8 页约中等；D10-B 反馈（设计已完、待开发）为增量；D12 视觉统一为收尾债。
- **风险**：真机 / 开发者工具验收需用户配合（沙箱无法运行微信小程序）；其余可在本地完成。
- **结论**：重建成本低且确定，不构成项目阻塞。

## 六、下一步建议
1. 用户核查微信公众平台代码包（最高优先级，约 10 分钟）。
2. 有 → 恢复并比对文档；无 → 进入 D12-A 第二步（建立版本归档规范）并排期重建。

---
*本报告为 D12-A 第一步交付物，未改动任何代码或线上资源。*
