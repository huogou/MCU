================================================================
MCU 抖音小程序 V1.2.0 提审前阻塞修复报告
执行方：开发 AI（Work）｜日期：2026-08-28 13:48
分支：douyin-migration @ commit 5f752e9
================================================================

一、背景与授权
- 前序《抖音小程序V1.2.0提审前检查报告_20260828.txt》判定「不通过」，列出
  致命 3 项（B1/B2a/B2b）+ 高 1 项（B3）+ 中 2 项（B4/B5）+ 低 2 项（B6/B7）。
- 用户于 13:48 明确授权「继续解决问题」，即对阻塞项执行修复。
- 全程仅改动 MCU-miniapp-douyin/（抖音版）；微信基线 mcu-miniprogram/
  与 wechat-production 分支零改动。
- 抖音 AppID 已由用户提供：tt00eb76569e914af801。

二、检查项目与结果（对照前序 14 项清单）
┌────┬──────────────────────────────┬──────────┬─────────────────────────────────┐
│ 项 │ 检查内容                     │ 本次结果 │ 说明                            │
├────┼──────────────────────────────┼──────────┼─────────────────────────────────┤
│ 1  │ 微信专属 API / 配置残留      │ 已修复   │ B1/B2a/B2b/B4/B5 全部处理（见三）│
│ 2  │ 页面跳转/返回/Tab 切换       │ 通过     │ 低风险 API 已于 Step2 迁移 tt.*  │
│ 3  │ 本地存储                     │ 通过     │ wx.getStorageSync→tt.*（Step2） │
│ 4  │ 图片资源加载                 │ 待真机   │ 静态引用无 wx.*，需抖音工具验证 │
│ 5  │ 宇宙时间线手机端显示         │ 待真机   │ 无 API 依赖，需抖音工具验证     │
│ 6  │ 关系探索                     │ 已修复   │ B2a 崩溃修复（explore.js）      │
│ 7  │ 影片详情及人物关系           │ 待真机   │ 无新增 wx.*                     │
│ 8  │ 观影进度                     │ 通过     │ userState 本地存储已迁移        │
│ 9  │ 分享海报                     │ 已修复   │ B2b 崩溃+保存链路重写（share.js）│
│ 10 │ 所有按钮及交互               │ 待真机   │ open-type="share" 抖音支持      │
│ 11 │ 控制台报错/Promise/404       │ 已修复   │ 崩溃 wx.* 已替换；无未处理 Prom │
│ 12 │ 隐私 API 不必要调用          │ 通过     │ 扫描无隐私 API 滥用             │
│ 13 │ 剪贴板调用范围               │ 通过     │ 全项目无剪贴板调用              │
│ 14 │ 测试/debug/console/临时文件  │ 已确认   │ B6/B7 已由 packOptions.ignore  │
│    │                              │ 无遗留   │ 覆盖（见三-5）                  │
└────┴──────────────────────────────┴──────────┴─────────────────────────────────┘

三、实际修改内容（5 文件，+46 / -47）
1) B1 — project.config.json
   · appid: wx78f00e7f0a5948b7 → tt00eb76569e914af801
   · projectname: mcu-miniprogram → mcu-miniapp-douyin
   （抖音开发者工具现可用此 AppID 打开工程编译）

2) B2a — pages/explore/explore.js（drawGraph）
   · L240  wx.createSelectorQuery().in(this) → tt.createSelectorQuery().in(this)
   · L249  wx.getSystemInfoSync().pixelRatio → tt.getSystemInfoSync().pixelRatio
   → 消除「进入关系探索页即崩溃」。

3) B2b — pages/share/share.js
   · L221  wx.createSelectorQuery().in(this) → tt.createSelectorQuery().in(this)
   · L225  wx.getSystemInfoSync().pixelRatio → tt.getSystemInfoSync().pixelRatio
   · L643-677 savePoster 整体重写：
       原 wx.canvasToTempFilePath（type=2d 不兼容）→
       新链路 canvas.toDataURL('image/png')
              → getFileSystemManager().writeFile({encoding:'base64'})
              → tt.saveImageToPhotosAlbum
              → 失败时 tt.openSetting 引导授权
     （依据抖音官方文档：canvasToTempFilePath 已停止维护且 V1 专用，
       2d 画布须用 toDataURL+writeFile）
   → 消除「进入分享页保存海报即崩溃/失效」。

4) B4 — app.json
   · 移除 "style": "v2"（微信专属组件样式，抖音不识别）
   · 移除 "lazyCodeLoading": "requiredComponents"（微信专属）
   · 移除 "sitemapLocation": "sitemap.json"（抖音不使用 sitemap）

5) B5 — sitemap.json
   · 删除文件（抖音小程序无 sitemap 机制，且原内容为微信格式）。

6) share.wxml L17 open-type="share" —— 经抖音开放平台官方文档核实，
   <button open-type="share"> 在抖音可正确触发 onShareAppMessage，
   故【保留不改】（原检查报告此处为误判，已更正）。

7) B6 / B7 —— 经核查已由 Step1 复制时继承的 packOptions.ignore 覆盖：
   · utils/check-home-conformance.js、utils/render-tab-icons.js（含 console.log）
     已在 ignore 列表，不进提审包；
   · assets/icons/tab 下 9 个 *_preview.png / _overview.png 已在 ignore 列表；
   · assets/preview、avatars、phases、hero、entries、backgrounds 文件夹已忽略。
   → 无需改动，无冗余文件进包。

四、遗留风险（须用户在抖音开发者工具真机验证）
⚠ 以下为「运行时行为」类，本 AI 无法在沙箱内驱动抖音 GUI 验证，
  须用户在抖音开发者工具中打开 MCU-miniapp-douyin/ 后实测：
  R1. createSelectorQuery().in(this) 在抖音 Page 上的节点选取行为
      （2d canvas 取 node 与 size）；
  R2. getSystemInfoSync().pixelRatio 在抖音返回值是否符合预期；
  R3. canvas.toDataURL + writeFile 实机导出/写文件成功率；
  R4. saveImageToPhotosAlbum 在抖音的 scope 授权与 openSetting 跳转；
  R5. 各页图片/时间线/全景图在抖音真机的最终视觉。
  以上若实机异常，按「只修问题不扩大范围」原则定点修复。

⚠ B3（高·功能降级，非崩溃）：feedback 的 wx.cloud 写库
  - 抖音端已正确守卫（typeof wx === 'undefined' → 走本地队列兜底），不崩溃；
  - 但「我要吐槽」提交后【不落云库】，数据仅存本机；
  - 三种策略待用户决策（见下方 AskUserQuestion）：
    A. tt.request + CloudBase HTTP API（与 H5/微信同集合，需建后端）
    B. tt.createCloud + 抖音云开发独立集合（需开通抖音云+envID）
    C. 维持本地兜底（不影响提审，但反馈不汇总）

五、Git 与备份
- douyin-migration 分支：5f752e9（本次修复）→ 25b718a（Step2）→ 7b6b78f（基线）
- wechat-production 分支：baf309b（只读基线，未动）
- 微信正式版 mcu-miniprogram/：零改动（Git 已提交 HEAD 完整无损）
- 备份位置：
  · backup/mcu-miniprogram-wechat-baseline-20260827/（微信基线完整备份）
  · 标签 wechat-v1.2.1-baseline / wechat-production
  · 抖音基线快照 commit 7b6b78f

六、结论
本轮修复消除全部「致命崩溃级」阻塞（B1/B2a/B2b）与配置级阻塞（B4/B5），
工程现已可在抖音开发者工具以 AppID tt00eb76569e914af801 打开编译。
B3（feedback 持久化）需用户决策数据策略后方能闭环；B6/B7 确认已覆盖无需改。
修复后是否「可直接提审」取决于第四节 R1–R5 真机验证结果，
建议用户在抖音开发者工具跑一遍运行检查后，再正式提交审核。
================================================================
