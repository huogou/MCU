# MCU观影导航 V1.2 首页重构 · 修改说明 + 自测报告

> 阶段：V1.2 视觉统一升级 · 第一阶段（首页）
> 依据：《MCU-V1.2-UI-Design-System.md》（QoderWork CN，唯一视觉标准）+ 《MCU-V1.2-首页重设计方案.md》
> 执行方：Work（开发 AI）  ·  日期：2026-08-25
> 本版替代 10:47 任务书草稿——按最终设计系统合规实现。

---

## ① 页面截图（待用户产出）

- **本 AI 无法运行微信开发者工具**，真机截图需由用户在 DevTools 编译 `mcu-miniprogram/` 截取首页后提供。
- 设计文档明确要求："确认后不直接进入开发，需先由用户在微信开发者工具中截图真机效果，设计 AI 据截图微调后再正式验收。"
- **提交物①（截图）状态：PENDING（待用户）**。代码与样式已就绪，截图到位后由设计 AI 微调、GPT 验收。

---

## ② 修改内容

| 文件 | 改动 | 说明 |
|------|------|------|
| `pages/home/home.wxml` | 重写 | 6 模块按重设计方案锁定：①品牌入口 ②继续观看(Hero) ③我的路线(Content) ④宇宙探索(三卡) ⑤热门角色(scroll-view 横滚) ⑥最近观看(scroll-view 横滚)；移除 `.explore-desc`/`.explore-arrow`、品牌区加图标占位；消费全局 `.card-hero/.card-content/.card-compact` |
| `pages/home/home.wxss` | 重写 | 全面消费全局 Token；三级卡片(24/16/12rpx)、5 级字号(48/34/28/24/20)、模块间距 48rpx、顶部/底部 64rpx；继续观看金色微渐变+金边框；我的路线 CTA 降级为 Ghost；探索入口三卡等分仅图标+名称；角色/最近观看横滚；组件规则**零裸 hex** |
| `app.wxss` | 增补 1 行 | 设计系统附录定义 `--gold-a40`（品牌装饰金），原全局 Token 序列遗漏，按标准补回（非设计判断） |
| `pages/home/home.js` | **未改动** | 数据结构/跳转逻辑完全不变；字段 continueCard/myRoute/exploreEntries/hotChars/recent 与 wxml 绑定逐一对齐 |

**未触碰**：数据层(CONTENT/ROUTES/CHARACTERS)、路由、其他 11 个页面、业务逻辑。

---

## ③ 是否符合 Design System（合规核查）

| 设计规范条款 | 实现情况 | 结论 |
|------|------|------|
| §3.8 全部 Token、禁写死 hex | home.wxss 组件规则 100% `var()`；仅 `app.wxss` 的 Token 定义块含 hex（符合"Token 定义允许"） | ✓ 合规 |
| §3.1 统一色板 bg `#080B12` / gold `#F2B233` | 全局 Token 已为 V1.2 值（设计侧在重制窗口落地），首页消费 | ✓ 合规 |
| §3.6 阶段色仅内容标识 | 仅角色头像/海报占位背景使用 `--p1~p6`；按钮/边框/UI 框架无阶段色 | ✓ 合规 |
| §4 5 级字号（48/34/28/24/20） | brand-title=display48 / card-title+movie-name=title34 / body28 / caption24 / mini20 | ✓ 合规 |
| §5 间距体系 | 模块间距 `--space-xl`(48) / 卡片内 `--space-lg`(32) / 顶底 `--space-2xl`(64) / 页面边距 `--page-x`(32) | ✓ 合规 |
| §6 三级卡片（24/16/12） | 继续观看=Hero(24) / 我的路线=Content(16) / 探索+角色=Compact(12) | ✓ 合规 |
| §6.3 卡片交互态 | 选中/激活用 `--gold-a20` 边框 + `--gold-a06` 渐变；按下 `opacity:0.85` | ✓ 合规 |
| §7 Icon 规范 | 探索入口图标 48rpx、`--gold-a40`；**glyph 暂用 Unicode 占位**（§5.2.3 允许），待设计提供 SVG | △ 部分（占位） |
| §8 按钮三级 | 继续观看=Primary(金实心,16rpx,88rpx全宽)；我的路线=Ghost(金文字)；每页仅 1 个 Primary | ✓ 合规 |
| §1.2/§3.3 视觉金字塔 | 继续观看为唯一焦点（Hero+金CTA），其余模块权重递减 | ✓ 合规 |
| 首页重设计方案 §1.4 结构映射 | 6 模块类名/数据映射与设计文档一一对应 | ✓ 合规 |

**结论：首页实现与设计系统高度合规，仅 Icon 字形为 Unicode 占位（设计文档明许可），待 SVG 资源到位后替换。**

---

## ④ 自测结果

| 检查项 | 方法 | 结果 |
|------|------|------|
| JS 语法 | `node --check home.js` | SYNTAX_OK |
| 裸 hex 泄漏 | grep `:#hex` home.wxss | NO_BARE_HEX |
| 数据绑定对齐 | wxml `{{}}` 字段 vs home.js 输出 | 全部吻合（continueCard/mainText/phaseText/progress*/buttonText/movieId；myRoute/name/nextName/nextId/progressText；exploreEntries/key/title/glyph；hotChars/id/name/initial/phase/count；recent/id/name/initial/phase） |
| 旧结构残留 | grep explore-desc/arrow/spacer-lg | CLEAN_NO_LEGACY |
| home.js 改动 | git diff | 空（零改动，符合"不改数据结构/逻辑"） |
| Token 完整性 | grep `--gold-a40` app.wxss | 已补回（line 64） |

---

## ⑤ 遗留问题（待设计/GPT/用户）

| # | 问题 | 责任方 | 说明 |
|---|------|--------|------|
| L1 | **真机截图** | 用户 | 需在 DevTools 截取首页，交设计 AI 微调后 GPT 验收（交付物①） |
| L2 | **Icon SVG 替换** | 设计 AI(QoderWork CN) | 品牌盾牌 + 探索三入口(◷✦◈) 当前为 Unicode 占位；设计系统 §7.5 指定 SVG 字形，待设计产出 SVG 资源后替换（含 TabBar 图标同步换线性 SVG） |
| L3 | **Hero Card 渐变真机渲染** | 用户+设计 | `linear-gradient(135deg,--gold-a06,--surface-1)` 在深色屏上明暗需在真机确认 |
| L4 | **48rpx Display 小屏溢出** | 用户+设计 | 真机验证品牌标题在 iPhone SE 等小屏是否溢出 |

---

## ⑥ 新旧差异（V1.1 → V1.2 首页）

| 维度 | V1.1 | V1.2 |
|------|------|------|
| 色彩 | 首页独立色值（双色彩体系） | 全局统一 `#080B12`/`#F2B233`（消除双色彩） |
| 字号 | 15+ 种混用 | 5 级制 48/34/28/24/20 |
| 卡片圆角 | 统一 32rpx | 三级 24/16/12rpx |
| 继续观看 | 普通卡片 | Hero Card + 金微渐变 + 金边框，全页唯一焦点 |
| 我的路线 | 与继续观看同权重 | Content 卡片 + Ghost 按钮（降权） |
| 宇宙探索 | 带描述+箭头 | 仅图标+名称（轻量化） |
| 热门角色/最近观看 | 平铺 | scroll-view 横向滚动 |
| 模块间距 | 16-20rpx | 统一 48rpx（呼吸感） |

---

## 提交与门控

- 代码已写入 `mcu-miniprogram/`（home.wxml / home.wxss / app.wxss 补 Token），`home.js` 未动。
- **门控**：本页交付后**等待 GPT 验收**；未经确认不进入第二阶段（路线页）。
- 待 L1 截图到位 → 设计 AI 微调 → GPT 验收通过 → 进入路线页。
