# V1.2 视觉校准 · Step ②④（浏览页 + 全局 Token）修改说明与自测报告

> 阶段：V1.2 视觉校准执行任务（GPT 14:16 指令，用户「按推荐执行」放行）
> 本轮范围：**④ 全局字体/间距/Icon/卡片体系 Token 升级 + ② 电影列表页（浏览页）视觉重构 + 图片资源层（visuals.js）接入**
> 依据：《MCU-V1.2-开发视觉标注》（§一 Token / §二 全局类 / §十 图片规范 / §十二 实施约束）
> 执行方：Work（开发 AI） · 日期：2026-08-25 14:30

---

## 一、修改文件清单

| 文件 | 改动 | 说明 |
|------|------|------|
| `app.wxss` | Token 体系升级 + 全局类更新 | ④ 全局统一（详见下表） |
| `data/visuals.js` | 填充 38 海报 + 38 剧照 URL 映射 | 图片资源层接入（复用 H5 在线资产） |
| `pages/browse/browse.js` | 注入 posterUrl + initial | 每部电影取 `mcuData.visual(id).poster` |
| `pages/browse/browse.wxml` | 电影行重构 | 左海报 image + 右 中文名/英文名/阶段/状态 |
| `pages/browse/browse.wxss` | 全面重写 | 按 V1.2-DS Token（Content 卡 20rpx/海报 96×144/状态胶囊） |

**未触碰**：数据模型、跳转逻辑、路由、其他页面、功能逻辑。

---

## 二、④ 全局 Token 升级（app.wxss，按《开发视觉标注》§一）

| 维度 | 旧值 | 新值 |
|------|------|------|
| 字号 | display 48 / display-sm 40 / title 34 / mini 20 | display **56** / display-sm **44** / title **36** / mini **22**（body 28、caption 24 不变） |
| 间距 | sm 16 / md 24 / lg 32 / xl 48 / 2xl 64 / page-x 32 | sm **20** / md **28** / lg **36** / xl **56** / 2xl **72** / page-x **36** |
| 新增 Token | — | `--surface-0 #0D1119`；功能四色 `--accent-blue/red/purple` + a10/a20；圆角 5 级 `--radius-sm/md/lg/xl/full`（12/16/20/32/999rpx）；阴影 `--shadow-card/hero/glow-gold/glow-gold-strong` |
| 三级卡片 | Hero 24 / Content 16 / Compact 12rpx | Hero **32**（+shadow-hero）/ Content **20**（+shadow-card）/ Compact **16**rpx |
| 全局类 | .mcu-title/sub/weak/btn-gold | 新增 .mcu-display/body/caption/mini/btn-primary/btn-ghost；btn-gold 圆角 token 化 |

> `--fs-display-sm` 因 7 页面在用，保留并按升级方向置 44rpx（标注未列但不可删，防破坏）；既有 gold/success/white/bg/phase alpha token 全部保留（页面在用），仅按规范升级数值。

---

## 三、② 浏览页重构（按 GPT 14:16 任务书 + V1.2-DS）

| 项 | 实现 |
|----|------|
| 左列 | **海报缩略图** 96×144rpx（2:3），圆角 16rpx，`<image>` aspectFill + lazy-load；兜底层=阶段色渐变 `poster-pN` + 首字（三态：加载中/失败显示兜底，成功 image 覆盖） |
| 右列 | 中文名 28rpx/600/main → 英文名 24rpx/sub → 阶段 24rpx/weak（≤3 行信息） |
| 状态 | 胶囊 chip：已看=success 绿 / 在看=gold 金 / 未看=weak 灰（语义对齐设计：绿色完成、金色进行中、灰色未看） |
| 行容器 | Content 卡片：surface-2 + 1rpx surface-3 + 圆角 20rpx + shadow-card + 内边距 20rpx |
| 分组 | 阶段标题 36rpx/600/main + 阶段色圆点 + 数量 |

---

## 四、图片资源层（visuals.js）

- 复用 **H5 已上线 38 张海报 + 38 张剧照**（CloudBase 静态托管，curl 实测 HTTP 200）
- 映射为绝对 URL：`https://mcu-d6gw0brqoa9521b58-1307093647.tcloudbaseapp.com/assets/{posters,stills}/{id}.jpg`
- **⚠️ 加载依赖**：需在 mp 后台将上述域名加入 **downloadFile 合法域名**（人工配置一次）；未配置前 image 加载失败 → 自动走 poster-pN 阶段色兜底，不破图
- 缺失资源（未收录 21 部/剧集）返回 null → 前端兜底（设计许可的降级）

---

## 五、自测结果

| 检查项 | 方法 | 结果 |
|------|------|------|
| JS 语法 | node --check visuals.js / browse.js | 均 SYNTAX_OK |
| 零 500 字重 | grep browse.wxss/app.wxss 工具类 | 无（仅 400/600/700） |
| 零裸 hex | grep browse.wxss 组件规则 | 无（全 var()；Token 定义块含 hex 属允许） |
| 绑定对齐 | browse wxml `{{}}` vs js 输出 | 全对齐（posterUrl/initial/status 新增字段均在） |
| 视觉预览 | 无头 Chrome 渲染（真实海报直链）| 375×1068，L=0/R=374 全宽、零断档、608 色桶=海报已加载 |
| 回归风险 | app.wxss token 变更影响面 | 全局按设计意图升级；display-sm 保留防 7 页破坏 |

---

## 六、遗留与依赖

| # | 项 | 责任方 | 说明 |
|---|----|--------|------|
| L1 | **downloadFile 合法域名配置** | 用户（mp 后台） | 未配置则浏览页/后续页海报显示兜底色块 |
| L2 | 缺失图片（21 海报/24 头像/6 阶段/1 背景/场景图） | 设计/用户 | ①首页与③资源接入依赖；就位后按 visuals.js 同名登记即生效 |
| L3 | 真机截图（浏览页） | 用户 | DevTools 编译 mcu-miniprogram/ 截取 |
| L4 | Icon SVG 资源 | 设计侧 | 当前按标注 §9.3 用 CSS/Unicode 占位过渡 |

---

## 提交与门控

- 本轮交付：④ 全局 Token + ② 浏览页重构 + visuals.js 资源层 + 视觉预览（HTML/PNG）
- 后续：①首页（待头像/背景图就位）→ ③资源接入 → ⑤ 全局统一收尾（Icon/间距复查）→ 提交 5 页截图验收
- 未验收通过前，不进入新功能开发
