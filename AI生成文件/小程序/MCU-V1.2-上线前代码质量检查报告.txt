MCU V1.2 微信小程序 · 上线前代码质量检查报告
生成时间：2026-08-27
范围：mcu-miniprogram/ 全量代码（15 页 / 0 自定义组件 / 55+ JS / 50+ WXSS）
角色：开发 AI（Work）
方法：程序化扫描（Node 审计脚本）+ 人工复核；只做检查 + 清理 + 规范化，
      未新增功能、未改 UI 设计、未改业务逻辑、未调数据结构。

============================================================
一、检查范围
============================================================
  页面数量：15（home / routes / route-detail / movie / explore / panorama /
            characters / character / browse / my-mcu / feedback / share /
            about / agreement / privacy）
  自定义组件：0（全部页面 usingComponents 为空）
  主要功能模块：观看顺序与路线 / 电影详情三态 / 关系探索 Canvas / 宇宙时间线 /
               角色图鉴 / 我的MCU 进度与成就 / 分享海报 Canvas / 反馈云写入 /
               用户协议·隐私政策·关于（审核页）
  检查方式：workspace/workspace-audit-launch-quality.js（require/console/wx:key/标签/分号）
           + workspace-audit-cdn-unusedclass.js（CDN 111 全量 / 未使用 class）
           + 定向 grep（密钥/开发地址/定时器/全局污染/懒加载/网络调用）

============================================================
二、已发现问题（按八类）
============================================================
【一、垃圾代码 / 测试代码】
  - 未使用 require：0 处（pages/models/data/app.js 全扫）
  - console.log / console.error / debugger：0 处（业务代码）
  - 测试按钮 / 测试入口 / 临时 mock：0 处（wxml 全扫；models 两个 _reset 已于此前清理）
  - 死样式（合规改造遗留，movie.wxss 6 条）→ 【已清理】见第三节
  - 废弃页面：0（15 页全部注册且可达）

【二、CSS 规范】
  - 缺分号：0 处真实问题（审计 2 处命中均为多行 linear-gradient 声明，
    复核确认均以 `);` 正确收尾，误报）
  - 行内 style：此前 8 处缺 `;` 已修复（home/browse/my-mcu/movie/route-detail/routes），
    本次复扫 14 处行内样式全部规范
  - 未使用 class：6 条死样式已清理（movie.wxss）；其余候选（status-*/hero-*/rel-*/
    ico-circle）均为 JS/wxml 动态拼接类名，确认在用
  - 重复样式 / 覆盖冲突：0（未发现同属性覆盖冲突；阵营/阶段色已统一 app.wxss）

【三、WXML 规范】
  - 标签闭合：0 真实问题（审计 9 处 image/input 为自闭合标签 `<image/>` 误报，
    WXML 规范允许）
  - wx:for 缺 wx:key：0 处
  - 空绑定 {{}}：0 处
  - 属性格式：合法（无非法写法；wx:if/wx:else/wx:for 结构正确）

【四、JavaScript 规范】
  - 语法：全部业务 JS node --check 通过（含本次改动）
  - 未声明/重复变量、全局污染：0（window./globalThis. 仅出现在 H5 适配注释中）
  - 异步错误处理：feedback.js 云写入带本地队列兜底；share.js 图片预载带
    pending 计数器 + 2.6s 超时守卫（settled 标志，无悬挂回调）
  - 生命周期：各页 onLoad/onShow 配置合理；movie/home 的 onShow 刷新为
    「从详情返回刷新状态」的既定模式，非重复执行；无 onUnload 需求
    （Canvas 2D 节点随页面销毁自动释放，无定时器/监听器残留）

【五、资源引用】
  - CDN：visuals.js 映射 111 张（海报38/剧照38/头像24/阶段6/Hero1/入口4）
    全量 HEAD 核验 111/111 返回 200，无 404
  - 本地资源：tab 图标 8/8 存在（app.json 引用全部落盘）；无废弃/重复/未使用
    本地资源（CDN 重复副本与预览图已通过 packOptions.ignore 排除出包）

【六、性能】
  - 首屏：数据本地加载（models 同步 require），无首屏网络请求依赖；
    图片均走 CDN，平均单张 ~6.7KB（0.75MB/111），首屏压力极小
  - 图片懒加载：滚动列表/横滚区已带 lazy-load；14 处未带 lazy-load 的为
    首屏图与短列表图（单页 ≤6 张），影响可忽略 →【建议，非阻塞】
  - Canvas：explore 关系网络 / share 海报均用 Canvas 2D 新 API，
    页面退出节点自动销毁，无重复绘制、无残留（share 有 pending 计数防竞态）

【七、安全与上线】
  - 测试 AppID：无（AppID wx78f00e7f0a5948b7 为正式账号，与后台一致）
  - 测试接口 / 开发环境地址：0（无 localhost/127.0.0.1/192.168；HTTP 仅
    wikipedia 数据源注释与自有 CDN）
  - 密钥泄露：0（sk-/AKID/secret/password/api_key 等 0 命中；"秘密战争/秘密入侵"
    为影片名，误报）
  - 后台调试入口：0；无 wx.request / 云函数 / downloadFile 调用
    （唯一网络调用 = feedback 云数据库写入，带兜底）

============================================================
三、清理内容
============================================================
  删除（本次审计）：
    - 死样式 6 条：pages/movie/movie.wxss
      .resource-icon / .resource-pending / .resource-item /
      .ri-label / .ri-btn / .cast-hover（合规改造后无任何引用的遗留 CSS）
  清理（此前会话已完成，纳入本报告累计）：
    - 行内 style 缺分号 8 处（7 个页面）
    - 开发脚本/预览图/CDN 重复副本 33 条 packOptions.ignore 排除
    - movie 页「高清在线观看」→「观看说明」合规改造
    - models 两个 _reset() 调试方法
    - movie.js 未使用 resources 导入
  新增（审计工具，仅开发侧，不影响包体）：
    - workspace/workspace-audit-launch-quality.js
    - workspace/workspace-audit-cdn-unusedclass.js

============================================================
四、当前状态
============================================================
  八类检查全部通过，无阻断性问题。
  唯一待决策建议（非阻塞）：14 处短列表/首屏 image 未加 lazy-load，
    平均单张 ~6.7KB 影响可忽略，如需极致优化可后续处理。
  代码质量结论：满足微信审核提交要求（代码层面）。
  提交前仍需人工完成（代码外，与此前一致）：
    ① 微信后台 downloadFile 合法域名添加 CDN
    ② 用户隐私保护指引填写关联
    ③ 按《审核截图规范清单》捕获 6 张截图
    ④ 名称/类目确认 ⑤ 版本号 V1.2.0 ⑥ 上传提交
